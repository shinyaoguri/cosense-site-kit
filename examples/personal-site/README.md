# personal-site (example)

A minimal site built on `cosense-site-kit`. Point `source.project` in
`cosense.config.ts` at any public Cosense project to try it.

```bash
npm install
npm run fetch
npm run dev
```
