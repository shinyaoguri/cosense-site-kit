---
"@cosense-site-kit/theme-default": patch
---

Code blocks now show syntax highlighting (via theme-utils' build-time Shiki) and line numbers. Adds CSS that switches the Shiki palette to the dark set under the dark skin and numbers each line via a CSS counter. The existing language label and copy button still work.
