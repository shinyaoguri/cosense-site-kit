---
"@cosense-site-kit/theme-default": patch
---

Render the site favicon. The Layout now emits `<link rel="icon" href={site.icon}>` from `virtual:cosense-site-kit/site` — the icon of the project's first (or home) page, matching Cosense's favicon behavior.
