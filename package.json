{
  "name": "cosense-site-kit",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "packages/*",
    "site"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present -w packages/core -w packages/astro -w packages/theme-utils -w packages/theme-default -w packages/cli",
    "dev": "npm run dev --workspaces --if-present -w packages/core -w packages/astro -w packages/theme-utils -w packages/theme-default -w packages/cli",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "npm run typecheck --workspaces --if-present -w packages/core -w packages/astro -w packages/theme-utils -w packages/theme-default -w packages/cli",
    "lint": "biome check .",
    "format": "biome format --write .",
    "changeset": "changeset",
    "release": "npm run build && changeset publish"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.0.0",
    "@changesets/cli": "^2.27.9",
    "@types/node": "^25.0.0",
    "tsup": "^8.3.5",
    "typescript": "^5.7.2",
    "vitest": "^4.0.0"
  }
}
