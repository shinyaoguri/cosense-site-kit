// How many entries the paginated list routes (/posts, /tags/[tag]) show per
// page. A plain constant for now; if sites want it tunable we can surface it
// through the .site structure later.
export const POSTS_PER_PAGE = 12;
